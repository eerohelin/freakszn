import type { Theme } from "./types";

export const THEMES: Theme[] = ["purple", "dark", "light", "system"];
export const NAV_ITEMS_ORDER = ["/", "/game", "/profile", "/match-history"]
export const ROLES: string[] = ["Top", "Jungle", "Mid", "Adc", "Support"]